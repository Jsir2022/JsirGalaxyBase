package com.jsirgalaxybase.modules.core.market.domain;

/** Explicit administrator decision for a custom-market delivery left in an uncertain state. */
public enum CustomMarketDeliveryResolution {
    RESTORE,
    COMPLETE
}
